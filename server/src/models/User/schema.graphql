type Query { 
    getListUser: [User]!
    getOneUser(id: Int!): User!
    getManyUser(ids: [Int]!): [User]!
}

type Mutation { 
    createUser(name: String!, groupId: Int): User!
}

type User { 
    id:            Int
    name:          String
    login:         String
    password:      String
    tasks:         [Int]
    groupId:       Int
}