type Query { 
    getListUser: [User]!
    getOneUser(id: Int!): User!
    getManyUser(ids: [Int]!): [User]!
}

type Mutation { 
    createUser(name: String!, groups: [Int]): User!
}

type User { 
    id:            Int
    name:          String
    tasks:         [Int]
    groups:        [Group]
}